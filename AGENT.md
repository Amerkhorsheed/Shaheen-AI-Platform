# 🤖 AGENT.md — دليل الوكلاء والذكاء الاصطناعي (AI Agent Handoff & System Manual)

> **وثيقة التوجيه التقني الشاملة لأي وكيل ذكاء اصطناعي (AI Agent) أو مطوّر يتولى العمل على هذا المشروع.**  
> تم إعداد هذه الوثيقة لتوثيق تاريخ الترحيل من حاسوب المحمول إلى الخادم، الترقية لقاعدة بيانات PostgreSQL الحاوية على Docker مع pgvector، التعديلات المعمارية، البنية الحالية للنظام، وكيفية التطوير والنشر.

> [!CAUTION]
> **🚨 قاعدة إلزامية وصارمة لجميع وكلاء الذكاء الاصطناعي (MANDATORY RULE FOR ALL AGENTS):**  
> عند قيام أي وكيل بإجراء **أي تعديل** على الكود البرمجي (في الواجهة `client` أو الخادم `server`)، **يجب إلزامياً فور إنهاء التعديل بناء ودفع التغييرات إلى حاوية دوكر (Push to Docker)**:
> 1. إعادة بناء الواجهة: `cmd /c npm --prefix client run build`
> 2. تحديث الحاوية: `docker compose up -d --build shaheen-ai`
> 3. التحقق من صحة الحاوية: `docker ps`  
> **إذا لم تدفع التعديل إلى دوكر، فلن يظهر التحديث للمستخدم على الشبكة المحلية.**

---

## 📌 1. نظرة عامة على المنظومة (System Overview)

- **اسم المشروع:** منظومة OSS للذكاء الاصطناعي (OSS AI Platform / Shaheen AI Platform).
- **الهوية والهدف:** منصة ذكاء اصطناعي سيادية محلية ومستقلة تماماً (100% Offline / Air-Gapped)، مصممة بالهوية البصرية الرسمية للجمهورية العربية السورية (النسر السوري الذهبي، الأخضر الزمردي، الورق الدافئ، الذهب المعتق).
- **نموذج الذكاء الاصطناعي:** يرتبط محلياً وحصرياً بخادم **LM Studio** المحلي على المنفذ الافتراضي `http://127.0.0.1:1234/v1` (النموذج النشط حالياً: `qwen-3.5-opus-glm-27b`).
- **المعمارية التقنية:**
  - **Backend:** Node.js (v20/v24), Express 5, `pg` (PostgreSQL client), JWT Authentication, Multer, SSE Streaming.
  - **Frontend:** React 19, Vite, Tailwind CSS, Lucide Icons, Remark-GFM, Highlight.js.
  - **قاعدة البيانات:** PostgreSQL 16 مع إضافة **pgvector** مدمجة وتعمل داخل حاوية Docker مخصصة باسم `shaheen-postgres` مع وحدة تخزين دائمة `shaheen_postgres_data`.
  - **النشر:** متاح عبر التشغيل المباشر (`start.bat` أو `node server/index.js`) أو عبر حاويات دوكر الكاملة (`docker compose up -d`).

---

## 🔄 2. سجل الترحيل والتحديثات المعمارية (Migration History)

### أ. الترحيل من الحاسوب المحمول إلى السيرفر (Laptop to Server)
1. **مسار المستخدم (`FIX 11`):** تم حل مشكلة المسافات في مسار المستخدم بنقل وتصحيح مجلدات الإضافات التالفة لضمان استقرار أدوات التشغيل.
2. **إعادة بناء المكتبات:** تم تثبيت نسخ ويندوز الأصلية من الحزم البرمجية وإعادة بناء الواجهة الأمامية React + Vite.

### ب. ترقية قاعدة البيانات إلى PostgreSQL 16 مع pgvector على Docker
تم تثبيت وتكوين حاوية PostgreSQL الرسمية مع امتداد `vector` لمعالجة النصوص والتضمينات (Embeddings / RAG):
- **اسم الحاوية:** `shaheen-postgres`
- **الصورة المستخدمة:** `pgvector/pgvector:pg16`
- **المنفذ:** `5432:5432`
- **المستخدم:** `shaheen_admin`
- **قاعدة البيانات:** `shaheen_ai`
- **وحدة التخزين الدائمة:** `shaheen_postgres_data`
- **ترحيل البيانات:** تم ترحيل كافة السجلات من SQLite السابقة (8 تصنيفات مؤسسية، حسابات المستخدمين مع التشفير، إعدادات النظام، القوالب، المحادثات، والرسائل وسجل التدقيق) بنسبة 100% ودون فقدان أي معلومة عبر سكربت `server/migrateToPg.js`.
- **مهايئ قاعدة البيانات (`server/db.js`):** تمت إعادة كتابته بالكامل ليعمل بنمط غير تزامني (`async/await`) مع محول استعلامات ذكي، وترقية المعاملات الذاتية عبر `AsyncLocalStorage`.

---

## 🏛️ 3. الإضافات الهندسية الكبرى (Key Architecture Features)

### 1. نظام التصنيفات المؤسسية الإلزامي (Mandatory Institutional Categories)
جدول التصنيفات في PostgreSQL:
```sql
CREATE TABLE IF NOT EXISTS categories (
  id TEXT PRIMARY KEY,
  name TEXT UNIQUE NOT NULL,
  description TEXT,
  code TEXT UNIQUE NOT NULL,
  clearance_level TEXT DEFAULT 'official', -- 'top_secret', 'secret', 'official', 'unclassified'
  icon TEXT DEFAULT 'Briefcase',
  color TEXT DEFAULT '#02443A',
  prompt_context TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```
**الأقسام المعتمدة المسبقة (Pre-seeded):**
1. `cat_exec` - **القيادة والإدارة العليا (`EXEC`)**: سري للغاية، لون ذهبي `#B79E6A`.
2. `cat_strategy` - **التحليل والدراسات الاستراتيجية (`STRAT`)**: سري، لون زمردي `#02443A`.
3. `cat_legal` - **الشؤون القانونية والمراسلات الرسمية (`LEGAL`)**: رسمي، لون أخضر `#2E6B4F`.
4. `cat_finance` - **المالية والموازنات العامة (`FIN`)**: سري، لون ذهب معتق `#8B6F3E`.
5. `cat_tech` - **التحول الرقمي وتكنولوجيا المعلومات (`TECH`)**: رسمي، لون أزرق `#1E40AF`.
6. `cat_audit` - **التدقيق الداخلي والرقابة والمتابعة (`AUDIT`)**: رسمي، لون فولاذي `#475569`.
7. `cat_hr` - **الموارد البشرية والشؤون الإدارية (`HR`)**: رسمي، لون بنفسجي `#6B21A8`.

### 2. التوجيه الذكي التلقائي للأقسام (AI Category Prompt Injection)
في ملف [`server/lmstudio.js`](file:///c:/Batter/Batter/server/lmstudio.js)، عندما يقوم أي مستخدم بإرسال استفسار، يتم البحث عن تصنيفه المؤسسي وإدراج محددات القسم تلقائياً في سياق النموذج (System Prompt)، مما يوجه النموذج للتخصص الفوري (قانوني، مالي، تقني، استراتيجي) حسب طبيعة عمل المستخدم.

### 3. إلزامية تصنيف المستخدمين والتحقق الصارم (Strict User Management)
- جدول `users` يربط كل مستخدم إلزامياً بـ `category_id`.
- **شرط إلزامي:** لا يقبل الخادم (`server/auth.js`) إنشاء أي مستخدم دون تحديد `category_id` صالح وموجود.
- حماية المشرف: منع حذف الحساب الشخصي للمسؤول أو سحب صلاحيته لمنع قفل المنظومة.

### 4. تطهير واجهة تسجيل الدخول (Production Real App Login)
- في [`client/src/components/LoginModal.jsx`](file:///c:/Batter/Batter/client/src/components/LoginModal.jsx):
  - حذف القيم الافتراضية المسبقة تماماً (`username` و `password` يبدآن فارغين).
  - حذف مربع تلميح حساب المشرف الافتراضي من صفحة الدخول.
  - إضافة زر إظهار/إخفاء كلمة المرور (`Eye` / `EyeOff`).
  - إضافة خيار "تذكر اسم المستخدم على هذا الجهاز" مع التخزين في `localStorage`.

### 5. لوحة إدارة الكوادر والتصنيفات (`UsersModal.jsx`)
- شريط إحصاءات لحظي (إجمالي الكوادر، النشطين، المشرفين، التصنيفات).
- بحث فوري وأزرار فلترة حسب القسم مع عداد الموظفين لكل قسم.
- جدول تفاعلي بالهوية البصرية السورية يعرض شارة القسم ولونه، درجة السرية، والمسمى الوظيفي.
- تبويب مستقل للهيكل التنظيمي لاستعراض الأقسام أو إضافة أقسام مخصصة جديدة.

### 6. منظومة مربعات التأكيد والتنبيه السيادية الموحدة (`ConfirmModal.jsx` & `DialogContext.jsx`)
- **إلغاء نوافذ المتصفح الافتراضية:** استبدال كافة استدعاءات `window.confirm` و `window.alert` القبيحة (التي كانت تكشف عنوان الآي بي الداخلي `192.168.1.161:3001 says...`) بمربعات حوار مصممة بهوية المنظومة الرسمية.
- **عناصر التصميم:** خلفية ضبابية داكنة (`#0A241C`)، ترويسة سيادية بشعار النسر، بطاقة مخصصة لإبراز العنصر المستهدف (اسم الجلسة، الحساب، أو النموذج)، وشريط تحذير صريح للعمليات التي لا رجعة فيها.
- **إمكانية الوصول (Accessibility):** دعم كامل للغة العربية (RTL)، والإغلاق بمفتاح `Escape`، والتأكيد بمفتاح `Enter`، والتركيز الآمن تلقائياً على زر الإلغاء في عمليات الحذف لتجنب الحذف الخاطئ.
- **خطاف الاستخدام الموحد (`useDialog`):** توفير دوال `confirm(...)` و `alert(...)` تعمل بنمط الوعود (`Promise-based`) دون الحاجة لكتابة كود إدارة الحالة (State) يدوياً في كل مكون.

---

## 📁 4. خريطة الملفات الرئيسية (Repository Structure)

```text
c:\Batter\Batter\
├── client/                     # واجهة المستخدم React + Vite
│   ├── dist/                   # الحزمة المبنية الإنتاجية (يخدمها الخادم تلقائياً)
│   ├── src/
│   │   ├── assets/             # شعار النسر السوري والرموز الرسومية
│   │   ├── components/
│   │   │   ├── LoginModal.jsx  # واجهة تسجيل الدخول النظيفة
│   │   │   ├── UsersModal.jsx  # إدارة الكوادر والتصنيفات المؤسسية
│   │   │   ├── Sidebar.jsx     # القائمة الجانبية مع شارة قسم ومسمى المستخدم
│   │   │   ├── ChatView.jsx    # واجهة المحادثة الرئيسية والبث الحي
│   │   │   ├── MarkdownRenderer.jsx # معالج النصوص والجداول والأكواد
│   │   │   └── SettingsModal.jsx # ضبط المنظومة ورابط LM Studio
│   │   └── services/api.js     # واجهة الاستدعاءات البرمجية للباك إند
│   └── package.json
├── server/                     # الخادم والواجهة البرمجية Express
│   ├── index.js                # المدخل الرئيسي وتشغيل الخادم على 0.0.0.0:3001
│   ├── db.js                   # مهايئ PostgreSQL و Pool ومحول الاستعلامات والتهيئة
│   ├── auth.js                 # التوثيق، الصلاحيات، وإدارة الكوادر والتصنيفات
│   ├── lmstudio.js             # الربط مع LM Studio وحقن سياق الأقسام
│   ├── upload.js               # معالجة ورفع وتلخيص ملفات PDF/Word/Excel
│   ├── exportService.js        # تصدير التقارير الرسمية و CSV
│   ├── templates.js            # القوالب الرسمية المعتمدة
│   └── migrateToPg.js          # سكربت ترحيل البيانات التاريخية إلى PostgreSQL
├── test/
│   ├── verification.js         # اختبارات التحقق من البنية وقاعدة البيانات
│   ├── test_api.js             # فحص واجهات البرمجة والربط الكامل مع LM Studio
│   └── test_db.js              # فحص اتصال واستعلامات PostgreSQL المباشرة
├── .env                        # متغيرات البيئة ورابط قاعدة البيانات
├── .env.example                # نموذج متغيرات البيئة
├── Dockerfile                  # ملف بناء حاوية دوكر الإنتاجية للواجهة والخادم
├── docker-compose.yml          # تكوين دوكر الشامل (PostgreSQL + pgvector + Shaheen AI)
├── start.bat                   # تشغيل المنظومة بنقرة واحدة (مع الفحص التلقائي لحاوية PostgreSQL)
├── package.json                # حزم وإعدادات الباك إند
├── README.md                   # التوثيق العام للمستخدمين
└── AGENT.md                    # هذا الدليل (لوكيل الذكاء الاصطناعي)
```

---

## 🌐 5. واجهات برمجة التطبيقات (API Endpoints Reference)

| المسار | الطريقة | الصلاحية | الوظيفة |
| :--- | :---: | :---: | :--- |
| `/api/health` | `GET` | عام | فحص حالة الخادم ونوع قاعدة البيانات المتصلة (PostgreSQL) |
| `/api/auth/login` | `POST` | عام | تسجيل الدخول (يعيد بيانات المستخدم وتصنيفه والرمز المميز) |
| `/api/auth/me` | `GET` | مستخدم | جلب الملف الشخصي الكامل مع بيانات التصنيف |
| `/api/categories` | `GET` | مستخدم | استعراض قائمة التصنيفات المؤسسية وعدد الكوادر في كل قسم |
| `/api/categories` | `POST` | مشرف (`admin`) | إضافة تصنيف مؤسسي جديد (اسم، كود، لون، درجة سرية، سياق ذكي) |
| `/api/categories/:id`| `DELETE`| مشرف (`admin`) | حذف تصنيف مخصص (بشرط عدم وجود مستخدمين فيه) |
| `/api/users` | `GET` | مشرف (`admin`) | استعراض كافة المستخدمين مع تصنيفاتهم وإحصائيات جلساتهم |
| `/api/users` | `POST` | مشرف / عام | إنشاء مستخدم جديد (**إلزامي تحديد `categoryId`**) |
| `/api/users/:id` | `PUT` | مشرف (`admin`) | تعديل الكادر، تغيير القسم، تعديل الصلاحية، أو إعادة تعيين كلمة المرور |
| `/api/users/:id` | `DELETE`| مشرف (`admin`) | حذف مستخدم وسجلاته نهائياً (محمي ضد حذف النفس أو آخر مشرف) |
| `/api/users/stats` | `GET` | مشرف (`admin`) | إحصائيات شاملة للكوادر والأقسام |
| `/api/templates` | `GET` | مستخدم | جلب نماذج المراسلات الحكومية المعتمدة |
| `/api/llm/models` | `GET` | مستخدم | فحص حالة الاتصال بـ LM Studio واستعراض النماذج المحملة |
| `/api/llm/chat` | `POST` | مستخدم | إرسال محادثة وبث الرد مع الحقن التلقائي لسياق قسم المستخدم |

---

## ⚡ 6. أوامر التشغيل والصيانة (Operational Commands)

### تشغيل المنظومة محلياً (مع التحقق التلقائي من PostgreSQL):
```powershell
# الطريقة الأسرع والموصى بها عبر ملف الدفعة
.\start.bat

# أو مباشرة عبر Node (تأكد من تشغيل حاوية shaheen-postgres أولاً)
node server/index.js
```

### تشغيل المنظومة بالكامل عبر Docker Compose:
```powershell
# بناء وتشغيل الحاويات (PostgreSQL + التطبيق) في الخلفية
docker compose up -d --build

# متابعة السجلات اللحظية
docker compose logs -f
```

### إعادة بناء الواجهة الأمامية (عند تعديل ملفات `client/src`):
```powershell
cd client
npm.cmd run build
cd ..
```

### تشغيل سويت الاختبارات والتحقق الشامل:
```powershell
cmd /c npm test
# أو
node test/verification.js
node test/test_api.js
```

---

## 🔒 7. إعدادات الشبكة ومنافذ العمل (Network & Ports)

- **منفذ المنظومة:** `http://localhost:3001` (يستمع على `0.0.0.0:3001` متاح عبر الشبكة المحلية).
- **منفذ PostgreSQL:** `5432` (`postgresql://shaheen_admin:SykS1XgFDneYaLaj8e26@127.0.0.1:5432/shaheen_ai`).
- **منفذ LM Studio:** `http://127.0.0.1:1234/v1` (داخل دوكر يتم توجيهه عبر `http://host.docker.internal:1234/v1`).
- **حساب المشرف الافتراضي المعتمد:**
  - اسم المستخدم: `admin`
  - كلمة المرور: تُولَّد عشوائياً عند أول إقلاع وتُطبع مرة واحدة في سجل الخادم، ولا تُنشر هنا.
    لاستعادتها لاحقاً: `npm run reset-admin-password`.
  - التصنيف: `القيادة والإدارة العليا` (`cat_exec`).

---

## 🚨 8. بروتوكول النشر الإلزامي لجميع الوكلاء (Mandatory Docker Deployment Protocol)

> [!CAUTION]
> **قاعدة عمل تشغيلية دائمة (Non-Negotiable Agent Rule):**
> نظراً لأن المنظومة يتم استخدامها واختبارها عبر حاوية Docker (`shaheen-ai-platform`) على المنفذ `3001` المتاح عبر الشبكة المحلية، **فإن أي تعديل برمجي يُجريه أي وكيل ذكاء اصطناعي في الكود يجب دفعه فوراً إلى دوكر.**
> 
> **خطوات النشر الإلزامية بعد أي تعديل:**
> 1. **بناء الواجهة الأمامية (إذا كان التعديل في `client/src`):**
>    ```powershell
>    cmd /c npm --prefix client run build
>    ```
> 2. **إعادة بناء وتحديث حاوية التطبيق في Docker:**
>    ```powershell
>    docker compose up -d --build shaheen-ai
>    ```
> 3. **التحقق من صحة عمل الحاوية (Healthcheck):**
>    ```powershell
>    docker ps
>    ```
>    تأكد من أن الحاوية تحمل الحالة `Up ... (healthy)`.
> 4. **فحص السجلات للتأكد من عدم وجود أخطاء:**
>    ```powershell
>    docker logs --tail 25 shaheen-ai-platform
>    ```
